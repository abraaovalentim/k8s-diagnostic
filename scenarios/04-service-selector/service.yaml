apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-correct
  template:
    metadata:
      labels:
        app: app-correct
    spec:
      containers:
        - name: nginx
          image: nginx:latest
---
apiVersion: v1
kind: Service
metadata:
  name: wrong-service
spec:
  selector:
    app: app-wrong
  ports:
    - port: 80
      targetPort: 80
