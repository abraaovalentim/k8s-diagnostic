   0% |                                                | (0/3, 0 it/hr) [0s:0s]                                                                                 33% |██████████████                              | (1/3, 2 it/min) [32s:1m4s]                                                                                 66% |█████████████████████████████                | (2/3, 5 it/min) [55s:11s]                                                                                100% |████████████████████████████████████████████| (3/3, 3 it/min)
AI Provider: ollama

0: Deployment 01-imagepullbackoff/bad-image()
- Error: Deployment 01-imagepullbackoff/bad-image has 1 replicas but 0 are available with status running
Error: Deployment "01-imagepullbackoff/bad-image" has replicas unavailable with status running.

Solution: 
1. Check image availability and pull policy.
2. Verify deployment configuration (e.g., image, port, env vars).
3. Run `kubectl rollout history` to inspect previous deployments.
4. Try rolling back or restarting the deployment using `kubectl rollout undo` or `kubectl apply`.
1: ConfigMap 01-imagepullbackoff/kube-root-ca.crt()
- Error: ConfigMap kube-root-ca.crt is not used by any pods in the namespace
Error: ConfigMap kube-root-ca.crt is not used by any pods in the namespace.

Solution: 
1. Check if the ConfigMap is created correctly using `kubectl get configmap kube-root-ca.crt -o yaml`.
2. Verify if the ConfigMap is being used by a pod's `spec.containers[0].imagePullSecrets` or `spec.volumes` fields.
3. If not, check if there are any pods in the namespace that are using this ConfigMap.
4. If no pods are using it, consider deleting the ConfigMap to avoid unnecessary resource usage.
2: Pod 01-imagepullbackoff/bad-image-78ffccb955-6hg5d(Deployment/bad-image)
- Error: Back-off pulling image "nginx:tag-inexistente"
Error: Unable to pull image "nginx:tag-inexistente" due to non-existent tag.

Solution: 
1. Check if the tag exists on Docker Hub or other registry.
2. If not, create a new tag for the desired version of Nginx.
3. Update your Kubernetes deployment YAML file with the correct tag.
4. Apply the changes using `kubectl apply`.

